import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  textInput: {
    borderColor: 'black',
    borderWidth: 2,
    width: 200,
    textAlign: 'center',
    margin: 10,
  },
  modalTitleText: {
    fontWeight: 'bold',
    fontSize: 24,
    textAlign: 'center',
    marginBottom: 40,
    margin: 30,
  },
});
